.env
*.local
.DS_Store
node_modules/

# openclaw.json da raiz e' a config real (copiada da VPS, contem secrets).
# O template versionado fica em config/openclaw.json.
/openclaw.json
/openclaw.json.bak*

# Dados locais dos agentes OpenClaw (sessions, sandbox, configs por agente).
/agency/
