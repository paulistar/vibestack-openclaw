services:
  openclaw-vibestack:
    image: ${OPENCLAW_IMAGE:-vibestack-openclaw:latest}
    build:
      context: .
      args:
        OPENCLAW_REF: ${OPENCLAW_REF:-main}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      #- HOME=/root
      - NODE_ENV=production
      - TERM=xterm-256color
      - OPENCLAW_GATEWAY_BIND=${OPENCLAW_GATEWAY_BIND}
      - OPENCLAW_GATEWAY_PORT=${OPENCLAW_GATEWAY_PORT}
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN}
      - GOG_KEYRING_PASSWORD=${GOG_KEYRING_PASSWORD}
      - OLLAMA_MODELS=/var/lib/ollama
      # Meta Ads CLI oficial le ACCESS_TOKEN/AD_ACCOUNT_ID; mantemos META_ACCESS_TOKEN
      # como alias historico para o .env nao mudar.
      - ACCESS_TOKEN=${META_ACCESS_TOKEN}
      - AD_ACCOUNT_ID=${META_AD_ACCOUNT_ID:-}
      - META_ACCESS_TOKEN=${META_ACCESS_TOKEN}
      # Claw3D — Studio Next.js + proxy WS pro gateway in-process.
      # Bindado em 127.0.0.1:3001 dentro do container; socat publica em
      # 0.0.0.0:CLAW3D_PORT. Como o bind e' loopback, o access-gate fica
      # desligado e o acesso via SSH tunnel funciona sem cookie.
      # UPSTREAM_ALLOWLIST precisa de localhost,127.0.0.1 (gateway in-process).
      - CLAW3D_PORT=${CLAW3D_PORT:-3000}
      - UPSTREAM_ALLOWLIST=localhost,127.0.0.1
      - PATH=/root/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
    volumes:
      # UM mount só, abrangendo tudo do OpenClaw
      - ${OPENCLAW_DATA_DIR}:/root/.openclaw
      # Ollama em mount separado (faz sentido — outro app)
      - ${OLLAMA_DATA_DIR}:/var/lib/ollama
    ports:
      - "127.0.0.1:${OPENCLAW_GATEWAY_PORT}:18789"
      # Loopback do host pra alcancar o ollama interno (opcional, SSH tunnel):
      #   ssh -N -L 11434:127.0.0.1:11434 root@VPS
      - "127.0.0.1:11434:11434"
      # Claw3D Studio — UI 3D dos agentes via Next.js:
      #   ssh -N -L 3000:127.0.0.1:3000 root@VPS  -> http://localhost:3000
      - "127.0.0.1:${CLAW3D_PORT:-3000}:3000"
    command:
      - "openclaw"
      - "gateway"
      - "--bind"
      - "${OPENCLAW_GATEWAY_BIND}"
      - "--port"
      - "${OPENCLAW_GATEWAY_PORT}"
      - "--allow-unconfigured"
